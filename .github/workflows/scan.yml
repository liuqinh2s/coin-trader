name: Contract Token Scan

on:
  workflow_dispatch:
  schedule:
    - cron: "*/15 * * * *"

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  scan-and-build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Check Crypto Mint token
        env:
          CRYPTO_MINT_GITHUB_TOKEN: ${{ secrets.CRYPTO_MINT_GITHUB_TOKEN }}
        run: |
          if [ -z "$CRYPTO_MINT_GITHUB_TOKEN" ]; then
            echo "CRYPTO_MINT_GITHUB_TOKEN is missing or not available to this workflow."
            exit 1
          fi

      - name: Run scan
        env:
          CRYPTO_MINT_GITHUB_TOKEN: ${{ secrets.CRYPTO_MINT_GITHUB_TOKEN }}
          CRYPTO_MINT_AUTO_DISPATCH: "true"
        run: python scripts/scan.py

      - name: Build site
        run: python scripts/build.py

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site/

  deploy:
    needs: scan-and-build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
